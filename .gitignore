# --- Go binaries ---
/server/regente-server
/server/regente-server.exe
/server/regente-cli.exe
/server/regente.exe
/agent/regente-agent
/agent/regente-agent.exe
*.exe~
*.exe
dist/

# --- Runtime state (NUNCA versionar) ---
*.db
*.db-shm
*.db-wal
/server/regente-state.db*
/server/regente.db*
/server/workspace/
/server/sessions/
/sessions/
/deploy/demo/sessions/

# --- Logs ---
*.log

# --- Env / secrets ---
.env
secrets.json
*.pem
*.key

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Claude Code: overrides pessoais (settings versionado é o settings.json) ---
.claude/settings.local.json
.claude/launch.json

# docs/site/ é build, mas VERSIONADO de propósito: é o jeito de alguém abrir a
# documentação e a referência de API SEM subir server nem toolchain (clona/baixa
# e abre o index.html). O CI regenera e falha se ficar defasado — ver
# .github/workflows/ci.yml (passo "docs site up to date").
